{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力：主窗口。所有网络请求均由 Rust 后端完成，前端无需 http/ws 权限。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}